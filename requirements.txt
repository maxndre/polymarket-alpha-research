# Core data stack
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=12.0

# Database & storage
SQLAlchemy>=2.0

# Machine learning (pipeline step 7 — non-linear causality)
scikit-learn>=1.3

# LLM API client (pipeline step 5 — semantic filtering)
openai>=1.0

# Visualisation
matplotlib>=3.7

# Graph (causal graph generation)
networkx>=3.0

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Async support (used in semantic filtering)
asyncio-throttle>=1.0
